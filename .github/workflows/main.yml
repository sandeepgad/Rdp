name: FASTEST RICH DESKTOP (CINNAMON CORE)

on:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-24.04
    timeout-minutes: 360

    steps:
      - name: 1. Install Cloudflared (Fastest)
        run: |
          wget -q https://github.com/cloudflare/cloudflared/releases/latest/download/cloudflared-linux-amd64 -O /tmp/cloudflared
          chmod +x /tmp/cloudflared
          sudo mv /tmp/cloudflared /usr/local/bin/cloudflared

      - name: 2. Install Cinnamon Core (Rich & Lite)
        run: |
          sudo apt-get update
          # Using 'cinnamon-core' instead of full environment to save 3+ minutes
          sudo DEBIAN_FRONTEND=noninteractive apt-get install -y --no-install-recommends \
            cinnamon-core \
            xrdp dbus-x11 x11-xserver-utils \
            gnome-terminal
          
          # Fix for Polkit Error (Pre-creating directory)
          sudo mkdir -p /etc/polkit-1/localauthority/50-local.d/
          
          # Permission Fix
          echo "[Allow Colord all Users]
          Identity=unix-user:*
          Action=org.freedesktop.color-manager.create-device;org.freedesktop.color-manager.create-profile;org.freedesktop.color-manager.delete-device;org.freedesktop.color-manager.delete-profile;org.freedesktop.color-manager.modify-device;org.freedesktop.color-manager.modify-profile
          ResultAny=no
          ResultInactive=no
          ResultActive=yes" | sudo tee /etc/polkit-1/localauthority/50-local.d/45-allow-colord.pkla

          # Configure XRDP
          sudo adduser xrdp ssl-cert
          echo "cinnamon-session" > ~/.xsession
          sudo sed -i 's/3389/3389/g' /etc/xrdp/xrdp.ini
          sudo service xrdp start
          echo "✅ Desktop Environment Ready"

      - name: 3. Install VS Code & Antigravity
        run: |
          # VS Code (Background download setup)
          wget -qO- https://packages.microsoft.com/keys/microsoft.asc | gpg --dearmor > packages.microsoft.gpg
          sudo install -D -o root -g root -m 644 packages.microsoft.gpg /etc/apt/keyrings/packages.microsoft.gpg
          echo "deb [arch=amd64,arm64,armhf signed-by=/etc/apt/keyrings/packages.microsoft.gpg] https://packages.microsoft.com/repos/code stable main" | sudo tee /etc/apt/sources.list.d/vscode.list > /dev/null
          
          # Chrome Download
          wget -q https://dl.google.com/linux/direct/google-chrome-stable_current_amd64.deb
          
          # Install Both
          sudo apt-get update
          sudo apt-get install -y code ./google-chrome-stable_current_amd64.deb

          # Create Shortcuts
          mkdir -p ~/Desktop
          cat > ~/Desktop/Antigravity.desktop <<EOF
          [Desktop Entry]
          Version=1.0
          Name=Google Antigravity
          Exec=google-chrome --app=https://antigravity.google/ --no-sandbox
          Icon=google-chrome
          Type=Application
          EOF
          chmod +x ~/Desktop/Antigravity.desktop
          cp /usr/share/applications/code.desktop ~/Desktop/
          chmod +x ~/Desktop/code.desktop

      - name: 4. Create User & Start Tunnel
        run: |
          sudo useradd -m -s /bin/bash runner
          echo "runner:Antigravity2026!" | sudo chpasswd
          sudo usermod -aG sudo runner
          
          # Start Tunnel
          /usr/local/bin/cloudflared tunnel --url tcp://localhost:3389 > tunnel.log 2>&1 &
          
          echo "Waiting for Link..."
          for i in {1..20}; do
            sleep 2
            TUN=$(grep -Eo 'https://[a-zA-Z0-9-]+\.trycloudflare\.com' tunnel.log | head -1)
            if [ -n "$TUN" ]; then echo "🌐 Tunnel: $TUN"; break; fi
          done
          echo "$TUN" > tunnel_url.txt

      - name: 5. CONNECT NOW
        run: |
          TUN=$(cat tunnel_url.txt)
          echo "==================================================="
          echo "   🔥 FASTEST RICH DESKTOP READY 🔥"
          echo "==================================================="
          echo "📲 Connect via RD Client App:"
          echo "   PC Name : localhost:3389"
          echo "   User    : runner"
          echo "   Pass    : Antigravity2026!"
          echo ""
          echo "👇 RUN THIS COMMAND IN MOBILE/PC FIRST:"
          echo "cloudflared access tcp --hostname ${TUN#https://} --url localhost:3389"
          echo "==================================================="

      - name: Keep Alive
        run: sleep 21600
